# 安全说明

**[English Version](./SECURITY.md)**

## 内存管理

### 密钥清零 (Key Zeroization)

本库对敏感密钥材料实现了自动清零：

- **Sm2KeyPair**：通过 `ZeroizeOnDrop` derive，当 `Sm2KeyPair` 被丢弃时，私钥会自动清零。
- **SessionState**：会话状态被丢弃时，主密钥与会话密钥会在 `Drop` 实现中被清零。
- **SessionKeys**：SM4 会话密钥在 `SessionKeys` 被丢弃时清零。

**注意**：克隆 `SessionKeys` 时，原对象在丢弃时仍会被清零；如有需要，克隆体需显式清零。

### 密钥材料处理

```rust
// 密钥在丢弃时自动清零
let keypair = Sm2KeyPair::generate()?;
let signer = Sm2Signer::new(&keypair)?;
// 当 'keypair' 与 'signer' 离开作用域时，
// 其敏感数据会被自动覆写为零
```

## 密钥生命周期

### 会话密钥

- 会话密钥由 SM2 临时密钥交换派生
- 最长会话时长：24 小时（会话票据的硬编码上限）
- 会话密钥存储在 `SessionKeys` 中，丢弃时清零

### 票据加密密钥

- 票据密钥（`TicketKey`）须定期轮换
- 轮换后使用 `TicketKeySet::remove_key()` 移除旧密钥
- 每个票据密钥可加密无限数量的票据，但建议每 90 天轮换一次

## 证书校验

### 证书链验证

本库执行完整的证书链验证：

1. 使用签发者的公钥验证每张证书的签名
2. 检查有效期（not_before、not_after）
3. 验证中间 CA 的 BasicConstraints CA:true
4. 通过 SAN 或 CN 回退进行域名匹配

### CRL 支持

- 通过 `verify_cert_crl()` 支持 CRL 验证
- 若未提供 CRL，则跳过吊销检查
- CRL 有最长有效期；过期的 CRL 会导致验证失败

### 证书续期

- 仅未过期的证书可被续期
- 续期后的证书拥有新的序列号，但保留主题与公钥
- 续期前不得吊销原证书

## TLS 握手安全性

### 会话恢复 (RFC 5077)

- 会话票据使用 SM4-GCM 加密
- 票据包含 24 小时的最长生存期
- 需要客户端认证的会话不可被恢复（安全策略）
- 会话存储后端（内存、SQLite、PostgreSQL、Redis）会追踪已签发的票据 nonce 并拒绝重复票据（重放防护）

### CertificateVerify

- 服务端使用证书私钥对握手摘要哈希签名
- 这证明服务端持有相应的私钥
- 将证书绑定到特定的握手过程

### ALPN

- 支持应用层协议协商（ALPN）
- 尊重服务端偏好（服务端从客户端列表中选择）

### 票据重放

服务端通过会话存储后端实现重放防护。当会话票据被用于恢复时，服务端会检查该票据 nonce 是否已签发过——若已签发，则拒绝恢复。不同后端在出错时的行为不同：

- **内存**：任何错误都拒绝恢复（fail-closed，失败时保持安全/关闭状态）
- **SQLite/PostgreSQL/Redis**：默认重放检查为 fail-open（出错时允许恢复）。如需 fail-closed 行为，请将存储配置为 `fail_closed: true`

### GCM 计数器

内部 GCM 计数器在 nonce 构造中为 32 位。虽然外部序列号为 64 位并检查溢出，但在实践中：

- 每个会话最多加密 2^32 条记录
- 对大多数应用而言，此上限永远不会达到

### SM2 ID

默认 SM2 ID（"1234567812345678"）用于签名。生产环境中，请确保你的 ID 符合 GM/T 0003 的要求。

## TLCP 协议

### 双证书体系

TLCP（GB/T 38636-2020）采用双证书体系：

- **签名证书**：用于认证与握手签名
- **加密证书**：用于密钥交换（SM2 ECDHE）

TLCP 服务端操作必须同时提供两类证书。

### 与 TLS 1.3 不同的记录层

TLCP 使用协议版本 0x0101，且**不**实现：

- 内部内容类型封装（RFC 8446 §5.4）
- KeyUpdate 机制
- 0-RTT 数据

`close_notify` 作为 APPLICATION_DATA 记录被加密，并在接收端被识别为 EOF。

### 会话恢复

TLCP 会话恢复使用会话 ID（而非 TLS 1.3 中的会话票据）。会话缓存采用 LRU 淘汰策略，容量可配置（默认：1024 个会话），TTL 为 24 小时。

## 流量密钥保护

应用层流量密钥（`write_traffic_secret`、`read_traffic_secret`）被包裹在 `Zeroizing<Vec<u8>>` 中，以确保密钥材料在丢弃时被清零。这可防止通过内存检查或核心转储造成密钥泄露。

## 最佳实践

1. **使用较短的会话生存期**：根据你的安全需求合理配置票据生存期
2. **定期轮换票据密钥**：轮换后使用 `TicketKeySet::remove_key()` 移除旧密钥
3. **完整校验证书**：始终提供 CA 证书并启用域名校验
4. **对敏感应用使用客户端认证**：为 mTLS 启用 `require_client_auth`
5. **监控过期的 CRL**：确保在 CRL 过期前刷新

## 报告安全问题

我们十分重视安全漏洞。感谢你为负责任地披露发现所做的努力。

### 披露流程

1. **请勿公开提交 issue。** 安全漏洞不应在公开的 GitHub issue 中讨论。

2. **通过 GitHub Security Advisory 报告。** 使用 GitHub 的 [私密漏洞报告](https://github.com/GM-Engineers/gm/security/advisories/new) 功能。这确保了与维护者的加密通信。

3. **提供详细信息。** 包括：
   - 漏洞描述及其影响
   - 复现步骤（如有 PoC 代码）
   - 受影响的版本 / 组件
   - 任何建议的修复方案

### 预期流程

| 阶段 | 时间线 |
|------|--------|
| 确认收到 | 3 个工作日内 |
| 初步评估 | 5 个工作日内 |
| 严重问题的补丁发布 | 14 天内 |
| 高危问题的补丁发布 | 30 天内 |
| 公开披露 (CVE) | 与报告者协调，通常在补丁发布之后 |

### 范围

以下组件属于我们安全计划的范围：

- **gm-der**：证书与密钥的 ASN.1 DER 编解码
- **gm-crypto**：SM2/SM3/SM4 密码学实现、KAT 自检测试
- **gm-sm9-rs**：SM9 标识密码算法——签名、加密、密钥交换（纯 Rust + GmSSL FFI 双后端）
- **gm-tls**：TLS 握手协议、记录层、会话票据处理、证书验证、CRL 检查
- **gm-ca**：证书颁发机构操作
- **gm-http-client**：支持 GM/TLS 的 HTTP 客户端

### 不在范围内

- 需要物理接触运行进程的问题
- 由非默认配置下无限制资源分配导致的拒绝服务
- 依赖项中无法通过我们 API 被利用的问题
- 实践中不可利用的理论性攻击

### 致谢

我们会在安全公告与发布说明中感谢报告者（除非你希望保持匿名）。我们目前不提供漏洞赏金计划。

### 受支持的版本

| 版本 | 是否支持 |
|------|---------|
| 0.1.x | :white_check_mark: |

仅最新发布版本接收安全补丁。
