// Shared library crate for fuzz targets (cargo-fuzz convention).
//
// Fuzz targets under `fuzz_targets/` currently depend only on the workspace
// crates (`gm-tls`, `gm-crypto`, ...) directly, so this library is unused.
// It is kept as a placeholder so the cargo-fuzz project layout stays complete.
